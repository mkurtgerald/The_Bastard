services:
  app:
    build:
      context: ..
      dockerfile: runtime/Dockerfile
    image: vms-test/runtime:local
    container_name: vms-test-runtime
    restart: unless-stopped
    ports:
      - "8765:8765"
